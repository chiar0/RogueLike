#ifndef GAME_CPP_MAIN_HPP
#define GAME_CPP_MAIN_HPP

#include "engine.hpp"

void input(engine *game); //gestisce l'input

/*
 //manca il menu
 mettere in public la max_y e la max_x
 implementare queste due nel codice delle funzioni
 fare una repo separata per display e engine (anche per il menu?)
 integrare questa repo con quella del progetto
 una funziona che ritorna un punto casuale della finestra del dungeon libero
 */

#endif //GAME_CPP_MAIN_HPP
